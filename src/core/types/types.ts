export type State = {
  editorReducer: {
    lineColor: string,
    lineWeight: string,
    instrumentName: string,
    mouseDownPosition: string,
    mainCtx: object,
    subCtx: object,
    canvasSize: {
      width: number,
      height: number
    }
  }
}

export type Context = {

}